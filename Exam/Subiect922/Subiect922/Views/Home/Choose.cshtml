@{
    ViewData["Title"] = "Choose Page";
}

<div class="text-center">
    <h1 class="display-4">Choose Type:</h1>
    <form method="post" action="">
        <button type="submit" formaction="@Url.Action("Hotels", "Home")" class="btn btn-primary">Hotels</button>
        <button type="submit" formaction="@Url.Action("Flights", "Home")" class="btn btn-secondary">Flights</button>
    </form>
</div>
